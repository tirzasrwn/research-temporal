{
  "name": "hello-temporal-ts",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript worker, workflow and activities for the fulfillGreeting Temporal demo",
  "scripts": {
    "build": "tsc",
    "start": "node dist/worker.js",
    "dev": "ts-node src/worker.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@temporalio/activity": "^1.13.0",
    "@temporalio/client": "^1.13.0",
    "@temporalio/common": "^1.13.0",
    "@temporalio/worker": "^1.13.0",
    "@temporalio/workflow": "^1.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
